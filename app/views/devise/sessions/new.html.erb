<% if flash[:notice] && flash[:notice] != "Signed out successfully." %>
  <div class="alert">
    <a class="close" data-dismiss="alert" href="#">&times;</a>
    <%= flash[:notice] %>
  </div>
<% end %>

<div class="session-form">
  <h2>Sign in to your account</h2>
  <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => 'clearfix', :id => "auth-form"}) do |f| %>
    <% if flash[:alert].present? %>
      <div id="error_explanation">
        <ul><li><%= flash[:alert] %></li></ul>
      </div>
    <% end %>

    <div class="input">
      <%= f.email_field :email, :placeholder => "Email", :class => "username-field" %>
    </div>
    <div class="input">
      <%= f.password_field :password, :placeholder => "Password", :class => "password-field" %>
    </div>

    <div class="actions clearfix">
      <span class="login-checkbox">
        <%= f.check_box :remember_me, :class => "login-checkbox" %>
        <%= f.label :remember_me, "Remember me", :class => "choice" %>
      </span>
      <%= f.submit "Sign in", :class => "btn btn-green pull-right mtopm30" %>
    </div>
  <% end %>
</div>

<p class="note">Don't you have an account? <%= link_to "Sign up", new_registration_path(resource_name) %></p>
<p class="note"><%= link_to "Forgot your password?", new_password_path(resource_name) %> </p>
